Here's a coding challenge #25: Implement a software design solution that supports Windows, OS X and Linux. Implement the code in your toolchain (see above) before creating a target client. (See #3340 for a good primer.)

Finally, write the program. Build the target client into the source C++/C#/JIT in order to save the source code into another source file so people can use it instead of using code from the source!

Let's