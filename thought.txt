Here's a coding challenge #48: Implement a artificial intelligence solution that can find everything there is in between a regular Python script and a good C-level C library. It can even do all those things for you - but only a subset of them will work and you'll still have to get the right ones. If your language can produce a good C library for this job, chances are it can.

Now, in a nutshell: a python script that finds nothing here but data from another